# English validation messages
validation:
  required: "This field is required"
  email: "This value is not a valid email address"
  min_length: "This value is too short. It should have {{ min }} character or more.|This value is too short. It should have {{ min }} characters or more."
  max_length: "This value is too long. It should have {{ max }} character or less.|This value is too long. It should have {{ max }} characters or less."
  uuid: "This value is not a valid UUID"
  choice: "The value you selected is not a valid choice"

post:
  title:
    required: "Post title is required"
    min_length: "Post title must be at least 3 characters long"
    max_length: "Post title cannot exceed 255 characters"
  content:
    required: "Post content is required"
    min_length: "Post content must be at least 10 characters long"
  status:
    invalid: "Post status must be either 'draft' or 'published'"

user:
  email:
    required: "Email address is required"
    invalid: "Please enter a valid email address"
    already_used: "This email address is already registered"
  password:
    required: "Password is required"
    min_length: "Password must be at least 8 characters long"
    max_length: "Password cannot exceed 255 characters"
  role:
    invalid: "User role must be either 'user' or 'admin'"